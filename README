Jackoff : Java BeanClient Client Api (not done yet)
--------------------------------------------

What is BeanStalkd?
http://xph.us/software/beanstalkd/

Download Junit:
  wget http://www.jarfinder.com/index.php/jars/versionInfo/42784

compile:
  javac -cp "/$HOME/junit-4.5.jar:." *.java

run sample app:
  beanstalkd -d -l 127.0.0.1 -p 11300
  java Jack

test:
  beanstalkd -d -l 127.0.0.1 -p 11300
  java -cp "/$HOME/junit-4.5.jar:." org.junit.runner.JUnitCore TestBeanstalk

obviously we are missing 90% of the api support but
this gets us going (at least it gets me going on my
project ;) )
